# CODEx Changelog

## 2026-05-12

- Created project layout under `riemannian-latent-text-refinement/`.
- Renamed the project directory to `structured-latent-text-refinement/` to match the GitHub repository.
- Promoted `structured-latent-text-refinement/` to the official standalone project.
- Added standalone project metadata in `pyproject.toml` and `CITATION.cff`.
- Added `docs/STANDALONE.md` with the independent Git remote setup.
- Copied the current Stage 2 implementation from `v1/` into `src/`.
- Added training and inference entrypoints under `scripts/`.
- Added YAML experiment snapshots and lightweight paper/example placeholders.
